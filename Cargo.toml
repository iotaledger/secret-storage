[package]

name = "secret-storage"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
thiserror = "2"
async-trait = "0.1"

[features]
default = ["send-sync-storage"]
send-sync-storage = []
